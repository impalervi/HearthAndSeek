---
name: Do not merge to main without explicit instruction
description: Never merge feature branches into main unless the user explicitly says "merge to main". "Change branch" or "deploy" does not imply merging.
type: feedback
---

Do NOT merge feature branches into main unless the user explicitly says "merge to main". Instructions like "change branch to main" or "deploy" do not imply merging. Always ask for confirmation before merging to main.
